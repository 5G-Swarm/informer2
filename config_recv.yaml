debug_mode: False
use_log: False
robot_id: 0

role_info:
    is_client: True
    id: 'edge-01'

network_info:
    # ip: '172.16.10.3'
    ip: '127.0.0.1'

    target_info:
        ip: '127.0.0.1'

message_info:
    img:
        is_tcp: True
        port: 10000
        width: 640
        height: 480
    cmd:
        is_tcp: True
        port: 11000